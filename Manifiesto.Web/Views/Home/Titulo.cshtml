@model IEnumerable<master_aimar.Entities.Entities.empresas>
@{  
    if (Model.Count() > 0)
    {
        foreach (var i in Model)
        {
            <h3>Empresa en Sesión @Html.DisplayFor(a => i.nombre_empresa)</h3>
        }
        
    }
}

